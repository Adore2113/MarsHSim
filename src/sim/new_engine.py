#--------------------imports-------------------------♡
import copy
#----------------------------------------------------♡


#def copy_state(state):
  # new_state = copy.copy(state)

    #--------------------systems---------------------♡
  #  if hasattr(state, 'heaters'):
  #      new_state.heaters = [heater.copy() for heater in state.heaters]

 #   if hasattr(state, 'radiators'):
  #      new_state.radiators = [rad.copy() for rad in state.radiators]

  #  if hasattr(state, 'amine_beds'):
  #      new_state.amine_beds = [bed.copy() for bed in state.amine_beds]

 #   if hasattr(state, 'solar_arrays'):
 #       new_state.solar_arrays = [array.copy() for array in state.solar_arrays]

    


#def step(old_state, dt_min=60):

#--------------------one timestep--------------------♡ 
   # new_state = copy.deepcopy(old_state)

